# Task 1.2 — Instructions (deviations from root)

Root naming convention applies (`adi-<domain>-<task>-<resource-name>`). For this task, the resource prefix is:

**`adi-1-2-`**

Example: `adi-1-2-model-abstraction`, `adi-1-2-circuit-breaker-sm`, `adi-1-2-bedrock-lambda-role`.
