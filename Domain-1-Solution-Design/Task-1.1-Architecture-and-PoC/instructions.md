# Task 1.1 — Instructions (deviations from root)

Root naming convention applies (`adi-<domain>-<task>-<resource-name>`). For this task, the resource prefix is:

**`adi-1-1-`**

Example: `adi-1-1-prompt-templates`, `adi-1-1-support-conversations`, `adi-1-1-bedrock-lambda-role`.
